---
topic: spec-design-yard PRD
updated: 2026-07-08T15:45
---

- (event) Discovery scans complete (docs/code/market); condensed digests saved to discovery-notes.md
- (event) Contradiction found: docs claim DB auto-save w/ login; code is localStorage-only (prisma unwired, auth mock, Save/Share buttons decorative) — needs user resolution
- (event) Market scan: defensible wedge = typed lintable YAML + bidirectional canvas + simulation, local-first; no competitor ships all three (closest: Ilograph one-way YAML, Eraser cloud DSL sync, LikeC4 rising)
- (decision) Product is human-first: views stay simple/trackable via abstraction+encapsulation, with drill-down to detail; vision = isometry network spec<->diagram<->code, fully connected
- (decision) Stakes: internal tool first; next phase decided by how adoption goes
- (decision) PRD scope: MVP for first internal release
- (decision) Persistence inverted: specs are repo-resident *.spec.yaml files (versioned/diffable/PR-reviewable, human+agent editable); app becomes editor/viewer over files; localStorage demoted to fallback; prisma/auth parked-or-deleted at design time (user brief captured in addendum A)
- (decision) AI-agent surface: CLI first (user open to suggestions); files-in-repo already make agents first-class editors
- (decision) UX/onboarding named the MVP weak spot: too many buttons, hard to orient; significant effort on UX/UI incl. first-use tutorials/tips (dismissable)
- (decision) Code edge of the network: post-MVP, and 'isomorphism to code' is an overstatement — vision language should be softer (spec<->diagram sync is the real contract; code is adjacency, not isometry)
- (decision) Working mode: Fast path
- (decision) Hierarchy in MVP: some hierarchy, not full-blown — drafting as single-level grouping (assumption to confirm)
- (decision) Run story: spec-yard is a standalone tool, NOT part of the target repo; on launch user opens a project folder where specs + all generated artifacts are saved
- (decision) CLI scope delegated to Claude -> picked: MVP CLI = lint (CI gate, exit codes); render/simulate CLI post-MVP
- (decision) Success = 4 faces: (1) complexity ceiling an architect can design, (2) fidelity of Claude-Code-built code vs envisioned when fed the spec, (3) architect design velocity, (4) onboarding speed for someone new to a system
- (decision) UX scope: remove decorative controls + progressive disclosure + first-use onboarding/tips
- (assumption) Export: in MVP; interpreting 'should be expensive' as 'shouldn't be expensive' (Excalidraw ships export utils, cheap to build) — PNG/SVG
- (event) Full PRD draft v1 written (Fast path): 5 feature groups G1-G5, 17 FRs, 7 NFRs, 4 success-metric faces SM1-SM4, 4 open questions; ~10 [ASSUMPTION] tags pending user review
- (decision) Core principle sharpened: encapsulation = modules with STRICT INTERFACES is the primary design requirement (connect through interfaces, not into internals); visual masking is secondary and may coexist; flat schema confirmed as artifact of current implementation, never a stated requirement (sketch 003 explored nesting)
- (decision) Interface model confirmed: exposed-member interfaces for MVP; named ports post-MVP with mechanical upgrade path (exposed component -> default port); interface-bloat lint warning included
- (decision) OQ1 resolved: hierarchy depth = TWO module levels (modules may contain submodules; submodules contain only components) — interpretation stated back to user for confirmation
- (change) PRD edit: core principle sharpened (encapsulation via strict interfaces is a design requirement, masking is its consequence); G2 rewritten FR8-11 as modules+strict interfaces; OQ1 marked resolved; OQ2 given decision criteria; OQ4 reframed as design-doc item
- (decision) Hierarchy terminology settled: 'two levels' = two encapsulation boundaries (module, submodule); system root and leaf components are NOT encapsulation boundaries; FR8 wording tightened accordingly
- (event) Reviewer gate ran (rubric: strong 0 crit/high; adversarial: 5 top findings; reconciliation: 4 nits). All findings resolved: addendum supersession note, FR4 no-write-over-unread-state rule, SM2 experiment protocol, NFR4 cross-origin guard, FR9 non-weakening quick-fix default, glossary + assumptions index added
- (decision) User decision: staged internal drops (G1 -> FR12 decorative-removal -> G2 -> G3 rest -> G4+G5); cuts slip from bottom; G2 never sacrificed; MVP-complete gate kept for the eval
- (decision) User decision: SM4 = system onboarding (understanding an unfamiliar architecture via spec-yard), as drafted
- (decision) NFR3 reinterpretation signed off: 'do not change YAML format' scoped to persistence work; G2 module schema change is additive-only, old specs stay valid
- (event) PRD finalized
