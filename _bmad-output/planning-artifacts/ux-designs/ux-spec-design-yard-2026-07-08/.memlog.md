---
topic: Spec-Yard UX — DESIGN.md + EXPERIENCE.md spines
updated: 2026-07-09T23:28
---

- (decision) Create intent confirmed; source inherited by reference: planning-artifacts/prds/prd-spec-design-yard-2026-07-07/ (PRD + addendum + discovery notes). First step per user: extract PRD + survey current app UI, then read back.
- (event) Intake extracts captured to .working/: extract-prd.md (PRD distillation incl. glossary, FR list, open questions punted to UX) and extract-current-ui.md (as-built survey: IA, feature inventory, visual tokens, rough edges).
- (assumption) Form factor: desktop-only, mouse-first web app (current build + PRD local-first desktop-class framing). To confirm with user before IA closes.
- (event) User confirmed intake read-back (PRD extraction + current-UI survey) as accurate; no gaps named.
- (decision) Working mode: coaching path — decisions walked together, creative tools woven in.
- (decision) Form factor locked: desktop-only for MVP; narrow-window behavior = graceful degradation only, not a designed experience. Confirms prior assumption.
- (decision) Brain dump / top principle: LEAN DESIGN — see only what you really need, in both menus and diagram; tools that let the user choose how much they see at any time. Named as the most important principle; anti-overwhelm is the core complaint against existing tools.
- (decision) Brain dump / layout vision: two panes side by side most of the time — diagram + text/details. Selecting a unit in the diagram surfaces all its relevant details in the detail pane. Diagram view must make diving INTO a module and back OUT effortless.
- (decision) Brain dump / reference: VHDL + Cadence Virtuoso (VLSI schematics) named as admired models for handling hierarchical systems. Specifics to probe.
- (decision) Brain dump / bidirectional line-level navigation: jump from a specific spec line to the corresponding diagram location and vice versa — named critical.
- (assumption) Brain dump / NEW beyond PRD (scope to resolve): (1) alternate view formats per scope — switch diagram from static/structural to functional or temporal view; (2) guided wizard that generates spec text via navigable menus + templates for safe add/modify/remove; (3) loose spec-diagram mapping detection with notification to lock it down. None of these are PRD FRs as stated.
- (event) Brain dump captured; probes pending: what 'selecting a model' covers (component vs module), what specifically is admired in VHDL/Virtuoso, what 'loose mapping' means concretely, functional/temporal view format, wizard placement vs existing Focus tab.
- (decision) Interaction-detail decisions DELEGATED to facilitator: right-click menus, drag behavior, scrolling, keyboard-vs-mouse balance, animation/motion, sound, tool talkativeness — decide per best practices + project context, tag as [ASSUMPTION] in spines, surface only what warrants surfacing. User will tune from real usage post-build rather than pre-speccing ('no shopping cart growth').
- (decision) VHDL/Virtuoso reference decomposed — ALL FOUR traits confirmed as design anchors: (1) descend/ascend hierarchy navigation with breadcrumb (diagram BECOMES module internals, not expand-in-place); (2) symbol-vs-schematic duality (module from outside = compact symbol showing only interface pins; internals = separate complete drawing — two honest representations, not one collapsed/expanded drawing); (3) entity/architecture separation (same scope, multiple representational views — maps to functional/temporal view wish); (4) port discipline (interface is the ONLY connection surface; violations physically inexpressible).
- (decision) Fifth admired trait (user-added): authoring by drag-and-drop, copy-paste, and quick-wiring of modules — especially LIBRARY modules, 'the equivalent of boilerplate spec'. Connects to the wizard/templates thread: a palette/library of reusable module templates as a first-class authoring path.
- (event) Selection-to-details coverage (any unit vs module-primary vs component-primary): user unsure — DEFERRED to detail-pane design step; resolve with wireframe showing module-details vs component-details side by side.
- (decision) Loose mapping defined — ALL FOUR situations count: (1) diagram content with no spec counterpart; (2) spec content hidden by current view (layers/collapse/filters) with no partial-picture indicator; (3) visual meaning not encoded (layout implies grouping the spec doesn't state); (4) stale positions/layout drift not written back. Full sync-health surface, not a single check.
- (decision) Alt-view angles — ALL FOUR resonate: sequence/message-order, pipeline/data-journey, live-simulation-as-view, state/lifecycle. PLUS user-added fifth: free-text explanation is tolerable for aspects genuinely hard to diagram — a prose view/annotation as legitimate representation (echoes VHDL behavioral description).
- (event) Pattern noted: user selects ALL options when offered capabilities, while naming LEAN DESIGN as top principle. Tension to manage: capabilities vs visible surface; resolve via scope staging (MVP-designed / directional / out) and via lean-by-default disclosure design.
- (decision) SCOPE: Line-level jump spec-line <-> diagram-element, both directions = MVP-designed. Click element -> editor scrolls/highlights line; cursor on line -> canvas highlights/centers element. (Docs already promised it; serves honest contract.)
- (decision) SCOPE: Loose-mapping/sync-health = staged. MVP designs three cheap detectors: unmapped-canvas-content, stale-positions, partial-picture indicator (view hides spec content). 'Layout implies unstated grouping' inference = directional note only.
- (decision) SCOPE: Alt views = view-switcher pattern designed in MVP with two entries: live-simulation-as-view (promotion of existing playback) + free-text aspects (prose as legitimate representation). Sequence/pipeline/state renderers = directional; switcher UX must anticipate them.
- (decision) SCOPE: Module LIBRARY PALETTE = MVP-designed (drag-drop/insert of boilerplate template modules, quick-wired through interfaces — the Virtuoso authoring soul). Menu-driven text WIZARD = directional note; templates carry most of its safe-authoring value.
- (event) Three IA/module-navigation wireframe variants rendered to .working/: flow-variant-a-expand-in-place (FR10 as written, detail drawer), flow-variant-b-descend (Virtuoso symbols-only + breadcrumb + right inspector column), flow-variant-c-hybrid (peek-in-place + descend-to-work, adaptive drawer, expanded sync-health chip). All validated Excalidraw files. User to pick direction, whole or by parts.
- (decision) Module navigation: ALL THREE models supported, tiered by gesture and intent. Glance = peek (select + Space / hover-pause; read-only overlay, zero layout shift, Esc dismisses). Work = descend on double-click (canvas becomes internals, breadcrumb + Esc ascends, boundary pins at canvas edge). Study-in-context = expand-in-place via explicit corner control/context menu (deliberate act; collapse control seals). Single-click = selection only, never navigation. Rule: peek never edits, expand-in-place never navigates, descend does both.
- (decision) Details placement: DOCKABLE — ships as adaptive drawer under the editor by default, user can dock it right as a persistent inspector column (VS Code panel-style). Adaptive content (component form vs module interface/members) in either dock position. Accepted cost: one more preference + designing both geometries.
- (decision) Editor-canvas coupling on descend: FOLLOW WITH A LEASH — editor auto-scrolls to and highlights the module's YAML block on descend, but never locks; free scrolling afterward, line-jump works across scopes. Courtesy scroll, not a cage.
- (decision) Sync-health surface: CHIP + ACTIVE NUDGE. Corner chip silent-check when honest, stateful on drift, click expands popover with per-drift resolve actions. Additionally: drifts that risk DATA LOSS (e.g. canvas content not in spec that would vanish on reload) raise a dismissible toast. Consultative by default, interruptive only when loss is at stake.
- (decision) Lean default CONFIRMED as drawn: editor + canvas + collapsed diagnostics dot-strip only. Summoned surfaces: Grid = canvas view in switcher; Layers = visibility control on canvas toolbar; Metrics = analysis view/panel on demand; Tree = folded into breadcrumb navigation. Resolves PRD FR14 [ASSUMPTION] core-vs-advanced grouping.
- (decision) View switcher: single dropdown (Static / Simulation / Aspects now; Sequence / Pipeline / State grayed as future). Views follow where you stand: descended into a module, every view (incl. Simulation) renders only that module's internals with traffic entering via boundary pins; whole-system view = ascend first. No widen toggle. (User approved after plain-language rephrase; note: avoid the term 'scope-bound' in UI copy — it failed comprehension with the primary user.)
- (decision) Library palette: insertion from BOTH sides. Canvas = drag template from edge flyout onto canvas (lands in current module when descended, wired via pins). Editor = equivalent 'Insert template' action writing the same boilerplate YAML at cursor. One template registry, two entry points.
- (event) IA/interaction round COMPLETE — all 7 feature axes decided by user (module nav x3 tiered, dockable details, leash-follow editor, chip+nudge sync health, lean default confirmed, views follow standing scope, dual-entry library). Next round: visual identity (DESIGN.md greenfield) — theme question + feel elicitation, then render contrasting direction boards.
- (decision) Theme: LIGHT + DARK, LIGHT DEFAULT. Light leads — diagrams read like paper/whiteboard, matching Excalidraw's native feel; dark available via toggle. Notable inversion of current build (dark-only, Excalidraw theme=dark via color-inversion filter). Light default actually simplifies canvas (colors authored for light render natively); full light chrome palette to be designed; dark becomes the derived theme.
- (override) CORRECTION (user misclick): Theme = DARK-ONLY, DONE WELL — supersedes prior 'light+dark, light default' entry. One theme as the identity, properly built: all tokens defined (incl. the four currently-undefined ones), contrast fixed, matches the IDE-adjacent context. Keep the existing Excalidraw dark-inversion approach; no light palette, no toggle.
- (decision) Visual references (dark, elicited): VS Code as acceptable BASELINE (IDE-dark familiarity); Notion admired for CLEAN SLATE (restraint, calm, uncluttered); Discord dark theme admired for its COLORS and its TABS/MENUS ORGANIZATION (warmer grays, friendly rounded organization).
- (decision) Keyboard-driven requirements (promoted from delegated to explicit): (1) COMMAND PALETTE a la VS Code Cmd+P — search for any command instead of menu navigation; (2) keyboard toggling of panels/windows on-off a la VS Code; (3) infinite canvas with keyboard FIT-TO-SCREEN a la Visio (Excalidraw canvas already infinite; wire fit shortcut); (4) Cmd+, opens settings.
- (event) Visual identity round: steer captured; next = render contrasting dark design-direction boards (VS Code kin / Notion-calm dark / Discord-warm / wildcard drafting-instrument) for user reaction.
- (event) Four dark design-direction boards rendered to .working/: direction-vscode-kin (charcoal instrument + command palette variant), direction-notion-calm (warm near-black clean slate + peek variant), direction-discord-warm (clubhouse grays/blurple + details drawer variant), direction-drafting-instrument (blueprint blue-black + simulation variant). Same hero screen, real acme-checkout content. User to react: whole board or parts.
- (decision) DESIGN axis 1 — Surfaces: WARM NEAR-BLACK (Notion-calm family, #191919/#202020/#2f2f2f warm cast). Quiet-desk register, not IDE-charcoal, not blueprint.
- (decision) DESIGN axis 2 — Accent: RESTRAINED BLUE (#3794ff family, VS Code kin). The familiar IDE 'active' blue at disciplined volume, on warm near-black Notion surfaces. Used for: selection rings, active tab/view markers, focused inputs, primary/quick-fix buttons, palette highlight. NOT for type colors or severity colors.
- (decision) DESIGN axis 3 — Density: CHUNKY (Discord register). Medium text with larger forgiving controls, pill chips, roomy buttons. Replaces the prototype's 9-11px squint-density. Composes with lean default: fewer surfaces visible, each one comfortable.
- (decision) DESIGN axis 4 — Shapes: SOFT, BORDERLESS (Notion calm). 6-8px radii, separation by whitespace/shade with borders nearly vanished. Composition note: pairing with CHUNKY density means larger controls carrying borderless separation — use background-shade steps (Discord's grouping trick) where pure whitespace would need more room than chunky controls allow.
- (decision) DESIGN axis 5 — Canvas feel: RESTRAINED SKETCH [facilitator's call, delegated by user]. Slight irregularity, sub-degree tilts, gentle wobble; hand felt, no marker squeak. Rationale: preserves inherited whiteboard identity without fighting the calm soft chrome; no engineering cost (Excalidraw roughness dialed low).
- (decision) User delegated all remaining session decisions to facilitator ('You pick that and the rest of this session's options for me'). Everything below decided by best practice, tagged [DELEGATED]; user tunes from real usage.
- (decision) [DELEGATED] DESIGN axis 6 — Type colors composed for the mix: Gateway teal #2dd4bf, Stage violet #a78bfa, Store green #4ade80, Brick rose #f472b6. Rationale: four distinct hues legible on warm near-black; Store moved OFF indigo (collided with #3794ff selection blue); Gateway moved OFF amber (collided with warning amber); red/amber stay reserved for severities. Color never sole carrier — type chips keep text labels (existing [Type] label contract).
- (decision) [DELEGATED] Typography/elevation/motion/sound: UI = system-ui stack at 13-14px chunky scale; YAML/ids/readouts = ui-monospace. Elevation = background shade steps; soft shadows only on floating layers (popovers, palette, peek). Motion = 120-180ms eases, no decorative animation, respect prefers-reduced-motion. Sound: none.
- (decision) [DELEGATED] Save semantics (resolves PRD FR3 incoherence): auto-save to disk, debounced ~1s, every write preceded by mtime/content-hash check (FR4). Dirty dot = buffer ahead of disk (debounce window or blocked write). Cmd+S = flush now. Status bar shows true file path + dirty dot. Conflict on external change -> reload/overwrite prompt per FR4.
- (decision) [DELEGATED] View-state location (resolves PRD OQ4; overrides FR10's per-spec persistence line, which the PRD's own adversarial review flagged): view state lives in a project-folder config (.spec-yard/ or spec-yard.config.yaml), keyed per spec file — collapsed/expanded modules, dismissed tips, drawer dock position, lint severity threshold. The spec keeps only structural data + coords. Keeps the honest contract free of PR diff noise.
- (decision) [DELEGATED] Empty state (resolves FR15): new/empty project greets with bundled sample offer + guided 'create your first component' + library palette prominent. Never a blank editor. Template picker IS the palette — no separate picker surface.
- (decision) [DELEGATED] Onboarding sample location (resolves G1xG3 conflict): sample ships as a read-only demo project OUTSIDE the user's project folder; tour runs on it without polluting user files; dismissed state in config; re-enable from Help.
- (decision) [DELEGATED] FR2 non-happy paths: no main.spec.yaml -> file list + prompt to create or pick; empty new folder -> FR15 empty-state flow; read-only folder -> read-only banner mode, all write affordances disabled (no lying UI).
- (decision) [DELEGATED] Export (resolves FR16 and/or): PNG/SVG saves to <project>/exports/ (canonical), completion toast offers open/download. Respects collapsed modules + hidden layers.
- (decision) [DELEGATED] Terminology standardized: 'connection' never 'flow'; 'project folder' never 'workspace'; panel = 'Diagnostics' with severities error/warning/info; no jargon in UI copy (e.g. 'scope-bound' banned — plain phrasing like 'shows the module you are in').
- (event) DISCOVERY COMPLETE. All axes decided (user-picked or delegated). Entering Finalize: spine distillation next.
- (event) Spines distilled (DESIGN.md 488 lines / 39 color tokens / 21 components; EXPERIENCE.md 409 lines / full IA + gestures + states + keyboard map + 3 protagonist journeys). Pass-1 coverage self-run: strong across flows/tokens/components/states/visual-refs. imports/ empty — nothing to reconcile. Next: reviewer gate offer.
- (decision) Reviewer gate: user opted for FULL PANEL — rubric walker + adversarial + accessibility, dispatched as parallel Opus subagents. Each writes review-{slug}.md; synthesis pipeline (validation-report.html + .md) to follow.
- (event) All 3 reviewers returned. Rubric: strong, 0C/0H/2M/8L (FR5 migration flow missing; UJ2 no lint-fail branch). Accessibility: 2C/6H/9M/5L (no canvas keyboard nav; no screen-reader position; 5 contrast failures on unfenced pairs). Adversarial: 2C/8H/12M/4L (double-click descend collides with Excalidraw native rename — code-verified; descend scene-swap lacks coordinate model; lean-default 'exactly three things' contradicted by own chrome; textarea can't deliver line-jump promises without editor-component swap). Synthesis next.
- (event) Validation report synthesized (validation-report.html/.md): deduped 4C/13H/22M/16L. Load-bearing convergence: the descend model fails engine feasibility by mouse (Excalidraw double-click collision) AND accessibility by keyboard (no binding, no SR position) simultaneously. Triage proposal next.
- (decision) Triage: APPLY PLAN AS PROPOSED. C1 descend affordances = Enter on selected module + enter-icon on symbol + palette command; double-click demoted to progressive enhancement (rename keeps native double-click). C2 descend = filtered render of the SAME spec coordinates, viewport fitted to module bounds, boundary pins at nearest viewport edge — no second layout, no new state. C3 canvas keyboard model committed: Tab/arrows traverse, Enter descend, Space peek (arbitration: only when canvas focused + module selected), Esc ascend/dismiss; every canvas op palette-reachable incl 'Go to node…'. C4 SR position: editor + drawer = canonical accessible equivalents; canvas labeled graphical view, never the only path. Plus all 13 highs + cheap mediums in one remediation pass; remaining findings logged accepted-with-rationale.
- (event) Remediation applied: 4C/13H/20M/16L fixed; 1 sub-item accepted (marker relabel refused per preservation discipline — correct). Token changes: danger #ef6d70 (5.07:1), accent-dim #122a40 (4.78:1), syntax-punctuation #83827b (4.56:1), border-node #6e6d68 (3.32:1 graphical), accent-strong fenced graphical-only, readout 13px, kbd 12px. Descend = Enter/icon/palette (double-click demoted); descend renders same coords filtered+fitted; canvas keyboard model + palette command inventory added; editor+drawer named canonical accessible equivalents. Next: key-screen mocks.
- (event) Four key-screen mocks rendered in final post-remediation tokens: key-lean-default (+command palette), key-descended-module (+adaptive drawer swap), key-safety-surfaces (conflict modal/drift popover/data-loss toast in precedence), key-simulation-view (+bottleneck state). All voice-checked, spine-section comment blocks included. Next: mock coverage walk with user, then promotion + polish + final.
- (decision) Mock coverage walk: user named EMPTY STATE (FR13/FR15 first-run) + LIBRARY PALETTE FLYOUT for additional mocks. Confirmed spine-only (built from spine tables alone): peek card, expand-in-place, expanded diagnostics panel, open file rail, Layers control, Metrics analysis view, Aspects view, drawer docked-right, settings.
- (event) Artifacts promoted: 6 key-screen mocks -> mockups/ (lean-default, descended-module, safety-surfaces, simulation-view, empty-state, library-palette); 3 IA wireframes -> wireframes/. Direction boards + extracts remain in .working/ as exploration record. Spine links to be updated to promoted paths in the closing pass.
- (event) spines finalized
- (event) Closing pass: 6 mockup links + 3 wireframe repaths wired; 6 lift-backs (clean/saved state, sample-offer register, ghost-only drop [richer preview DELEGATED], palette card anatomy, diagnostics-green-during-bottleneck, reduced-motion gates glow); editorial standards applied (minimal — docs already compliant); DESIGN.md 549 lines / EXPERIENCE.md 617 lines, both status: final.
